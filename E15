using UnityEngine;

public class E15 : MonoBehaviour
{
    string estadoEnemigo;
    bool veJugador;
    bool estaCerca;
    bool tieneEnergia;
    
    void Start()
    {
        estadoEnemigo = (veJugador && tieneEnergia) ? "Perseguir" : (estaCerca && tieneEnergia) ? "Atacar" : "Esperar";

    }

    
    void Update()
    {
        
    }
}
