using UnityEngine;

public class E12 : MonoBehaviour
{
    string acceso;
    public bool tieneVip;
    public int nivel;
    public bool enCombate;

    void Start()
    {
        acceso = (((nivel >= 10) || (tieneVip)) && (!enCombate)) ? "Acceso permitido" : "Acceso denegado";
        Debug.Log(acceso);
    }


    void Update()
    {

    }
}
