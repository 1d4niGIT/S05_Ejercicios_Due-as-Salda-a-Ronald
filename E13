using UnityEngine;

public class E13 : MonoBehaviour
{
    string estado;
    int stamina;
    void Start()
    {
        estado = (stamina > 70) ? "Sprint" : (stamina > 30) ? "Run" : "Walk";
    }

    
    void Update()
    {
        
    }
}
