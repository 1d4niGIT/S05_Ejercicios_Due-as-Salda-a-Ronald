using UnityEngine;

public class E16 : MonoBehaviour
{
    public int health;
    public int stamina;
    string state;

    void Start()
    {
        GetPlayerState(health, stamina);
        Debug.Log(state);
    }
    string GetPlayerState(int health, int stamina)
    {
        return state = (health <= 0) ? "Dead" : (stamina < 20) ? "Tired" : "Ready";
    }
    void Update()
    {

    }
}
